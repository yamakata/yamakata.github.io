# CLAUDE.md

## このリポジトリについて
東京大学 情報基盤センター 山肩研究室の公式ウェブサイト。
Hugo（extended）+ Blowfish テーマ による静的サイト。
制作者は Web 制作が初めてなので、生成した設定の意味は日本語で説明すること。

## 絶対に守ること
- テーマ本体（`themes/` 配下、または Hugo Modules のキャッシュ）を直接編集しない。
  カスタマイズは必ずプロジェクト側の `layouts/` `assets/` `i18n/` でオーバーライドする。
- CMS・サーバサイド実行環境は導入しない。生成物は完全な静的ファイルであること。
- URL 構造を変更する場合は必ず事前に確認を取る。既存 URL は壊さない。
- 業績データは `data/` 配下の生成物であり、手書きしない。元データは `content-source/publications.bib`。
- 日本語コンテンツは `content/`、英語は `content.en/`（または設定に従う）に置く。
  片方だけ追加して終わりにせず、対応する言語のプレースホルダも作る。
- 研究室メンバーの氏名・経歴・業績を推測で書かない。不明な箇所は `TODO(確認):` を残す。

## やってはいけない操作
- `main` ブランチへの直接 push（作業ブランチを切って PR を作る）
- `git push --force`、`git reset --hard` などの破壊的操作
- 秘密情報（トークン、APIキー）のコミット
- `public/` `resources/` のコミット

## コマンド
- 開発サーバ: `hugo server -D`
- 本番ビルド: `hugo --minify`
- 業績データ生成: `python scripts/bib2yaml.py`

## コーディング方針
- Hugo のテンプレートは partial に分割し、1ファイル100行を超えない。
- 画像は `assets/` に置き、Hugo の image processing 経由で出力する。`static/` に直接置かない。
- ハードコードした文言はテンプレートに書かず `i18n/*.yaml` に置く。
- コミットは論理単位で細かく。メッセージは日本語で可。

## 作業の進め方
- ファイルを3つ以上変更する見込みのタスクは、まず計画を提示し、承認を得てから実装する。
- ビルドが通らない場合、エラーを回避するために設定を古い書式へ落とさない。エラー内容をそのまま報告する。
- 判断に迷う設計上の選択が出てきたら、勝手に決めずに選択肢を提示する。

## 変更時の完了条件
- `hugo --minify` がエラー・警告なしで通る（実際に実行して確認する）
- 日本語版・英語版の両方でビルドされる
- 変更点の要約と、各ファイルの役割を日本語で報告する